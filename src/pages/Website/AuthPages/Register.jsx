/**
 * Register.jsx — thin wrapper that renders the new multi-step wizard.
 * The wizard lives at src/features/auth/register/RegisterPage.jsx
 */
import RegisterPage from "@/features/auth/register/RegisterPage";

export default function Register() {
  return <RegisterPage />;
}